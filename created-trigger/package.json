{
  "name": "created-trigger",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx tsc && cp package.json ./build/package.json && cd build && npm i --omit=dev"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.108",
    "@types/dynamodb": "^1.3.2",
    "typescript": "^4.8.4"
  },
  "babel": {
    "presets": [
      "@babel/preset-env",
      "@babel/preset-typescript"
    ]
  },
  "dependencies": {
    "-": "^0.0.1",
    "aws-sdk": "^2.1241.0",
    "aws4": "^1.11.0",
    "@middy/core": "^4.0.0",
    "aws4-axios": "^2.4.9",
    "axios": "^0.27.2",
    "@aws-lambda-powertools/logger": "^1.4.0"
  }
}
