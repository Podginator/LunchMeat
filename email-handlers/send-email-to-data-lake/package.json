{
  "name": "register-message-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx tsc && cp package.json ./build/package.json && cp template.html ./build/template.html && cd build && npm i --omit=dev"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/dynamodb": "^1.3.2",
    "@types/mailparser": "^3.4.0",
    "typescript": "^4.8.4"
  },
  "babel": {
    "presets": [
      "@babel/preset-env",
      "@babel/preset-typescript"
    ]
  },
  "dependencies": {
    "aws-sdk": "^2.1241.0",
    "mailparser": "^3.5.0",
    "rxjs": "^7.6.0"
  }
}
